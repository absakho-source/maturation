# Configuration Email - Exchange/Office 365
# Copiez ce fichier en .env et remplissez avec vos identifiants

# Serveur SMTP Exchange
SMTP_SERVER=smtp.office365.com
SMTP_PORT=587

# Identifiants de connexion Exchange
SMTP_USERNAME=votre-email@dgppe.sn
SMTP_PASSWORD=votre-mot-de-passe

# Email d'envoi (généralement le même que SMTP_USERNAME)
FROM_EMAIL=votre-email@dgppe.sn
FROM_NAME=DGPPE - Plateforme PLASMAP

# URL de la plateforme
PLATFORM_URL=https://maturation-dgppe.onrender.com

# Activer/désactiver les emails (true/false)
EMAIL_ENABLED=true
EMAIL_DEBUG_MODE=false
